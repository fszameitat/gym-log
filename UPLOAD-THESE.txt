Upload the CONTENTS of this folder (not the folder itself) so that
index.html lands at the top level of the repository.

This is v6. The service worker cache is gymlog-v6, so a phone that
already has v5 picks it up on its next online load.

New in v6:
  js/catalog.js            your gym's 151 exercises, searchable
  bodyweight + cardio      units, so push-ups and rowing can be logged
  empty entry boxes        last session's numbers show as a grey ghost
  deletions stick          the starter library never reappears by itself

This version contains NO training data and NO plan of yours. Your
FitPlan Pro routine lives in MY-fitplan-routine.json one folder up -
that file is private. Never add it here.
